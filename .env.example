# Environment Variables Template
# Copy this file to .env and fill in your actual values
# NEVER commit .env with real values to git!

# ===== REQUIRED FOR PRODUCTION =====

# Upstash Redis (KV) Configuration - Get from Vercel KV or Upstash dashboard
KV_REST_API_URL=https://your-redis-instance.upstash.io
KV_REST_API_TOKEN=your-redis-token
KV_REST_API_READ_ONLY_TOKEN=your-readonly-token

# Alternative Redis URL format (may be provided by some services)
KV_URL=rediss://default:token@your-redis-instance.upstash.io:6379
REDIS_URL=rediss://default:token@your-redis-instance.upstash.io:6379

# AI Integration - Your N8N webhook or AI service endpoint
WEBHOOK_URL=https://your-n8n-instance.com/webhook/your-webhook-id
NEXT_PUBLIC_WEBHOOK_URL=https://your-n8n-instance.com/webhook/your-webhook-id

# ===== OPTIONAL =====

# Supabase Configuration (currently not used, but configured for future)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# ===== SETUP INSTRUCTIONS =====
# 1. Create Upstash Redis database (or use Vercel KV)
# 2. Copy Redis connection details to KV_* variables
# 3. Set up N8N webhook or AI service and add URL to WEBHOOK_URL
# 4. Deploy to Vercel and add these same variables in Project Settings > Environment Variables